# Next.js example (stub)

This will host an end-to-end demo: `lore init → annotations → check → sync → publish (local lore-board)`.

Currently a placeholder. PRs welcome — see [contributing/setup](https://lore-ai-docs.vercel.app/contributing/setup).
